{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --stream",
    "format": "lerna run format"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.9"
  }
}
